name: CI

on:
  push:
    branches: [main, live-events-manifest]
  pull_request:
    branches: [main, dev, live-events-manifest]

# Cancel in-progress runs when a new push is made
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint-and-build:
    name: Lint, Format Check & Build
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run security audit
        run: npm audit --audit-level=moderate

      - name: Validate provider catalog
        run: npm run catalog:validate

      - name: Check generated provider report
        run: npm run catalog:report:check

      - name: Run tests
        run: npm test

      - name: Check formatting
        run: npx prettier --check src

      - name: TypeScript build
        run: npm run build

      - name: Check for build artifacts
        run: |
          if [ ! -d "dist" ]; then
            echo "Error: dist directory not created"
            exit 1
          fi
          echo "Build successful!"
